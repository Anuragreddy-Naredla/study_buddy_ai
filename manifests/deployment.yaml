apiVersion: apps/v1
kind: Deployment
metadata:
  name: study-buddy-application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: study-buddy-application
  template:
    metadata:
      labels:
        app: study-buddy-application
    spec:
      containers:
      - name: study-buddy-application
        image: anuragreddynaredla/studybuddy:latest
        ports:
        - containerPort: 8501
        env:
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: groq-api-secret
              key: GROQ_API_KEY
